# Availability examples

These tiny JSONL fixtures are synthetic and offline. `clean.jsonl` should pass
when audited at `2024-01-31T00:00:00Z`; `contaminated.jsonl` is an intentional
negative path with post-cutoff availability and observation.
